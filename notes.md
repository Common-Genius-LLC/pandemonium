My notes on the app changes : 
1. Editing the section, de-links that section of text, this needs to be fixed
2. No way to delete a script draft
3. The text selection in script in still broken, please work on this core feature properly and get this fixed first, before moving to any other task
The bug is divided into the following points : 
a. The context menu sometimes doesn't appear when selecting the text
b. when the text is selected and relink button is clicked, it gives the warning (Select the passage in the script to reattach this board), when the text is already selected
c. maybe do something like dividing the script into different sections like a dialogue is one section and an action is one section and another is one section and that section has a background highlight of small rounded corners when it's hovered and the buttons to link the resource or storyboard appears and there's a button for comments too, you can break these by maybe line break, wherever there is line break, a new section starts, this would be quick for users and accidentally selecting just one word won't show up on the slideshow menu, I maybe wrong with this approach of separating using line break but you can also check on other open source projects like vs code that do this. and check more on github or the internet on how people achieve this.
d. Please specify what each button does in the Storyboard panel, and add an unlink button as well that unlinks a storyboard panel from the text. also the caption textbox shouldn't be there since the caption is the section it's linked to.
e. the boards and research switch should only appear when in split mode, and otherwide provide a dropdown for each menu under what it is, remove the titles (thumbnails, script and research) and replace them with a dropdown that allows to switch the panel to whatever the user wants.
f. make the panels in "everything" and "split mode" resizeable 
g. move the add new script to script panel
h. move the start slideshow to thumbnails panel and give it pink color used in the application.
i. remove the line that appears when clicking on a linked note, the note should note have any panel since we're changing this to comments, put it right inside the script on clicking the section the comment is added to, just like thumbnails of script appear.
j. the functionality for sources and links is also mixed up right now, they both show the same menu which is overflowing it's dialogue box, the comment should open the comment box and not a menu anyways, and remove that line from the source menu as well.